package com.mutual.liberty.sessionmanagement.dto;

import org.springframework.web.servlet.mvc.method.annotation.MvcUriComponentsBuilder;
import org.springframework.web.util.UriComponentsBuilder;

public class URIBuilderExample {
	/*
	 * 
	 * public static void main(String[] args) { int port = 8080;
	 * 
	 * // Use WebMvcLinkBuilder to build the URI UriComponentsBuilder uri =
	 * MvcUriComponentsBuilder .fromMethodName(URIBuilderExample.class,
	 * "dummyMethod") .scheme("http") .host("localhost") .port(port); // Print the
	 * resulting URI System.out.println(uri); } public void dummyMethod() { // This
	 * method is empty and won't be called, it's just for referencing in
	 * MvcUriComponentsBuilder }
	 */}
